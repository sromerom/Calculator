import javax.swing.*;
import java.awt.*;

public class Keypad{
    public enum tipusKeypad {
        NORMAL,
        BINARI,
        HEXADECIMAL,
    }

    JPanel panel;

    Keypad (String tipus) {

    }

    void normal() {
        /*
    Cardlayout cl;
    cl.show(panel, "Keypad panel")

    constructor
    cl = new CardLayout();
    panel.setLayout(cl);

    KpNormal k1 = new KpNormal();
    panel.add(k1.setPanel(), "KeyPad normal")

    */
    }
}
